program: src/models/train_model.py
method: grid
metric:
  goal: maximize
  name: val_accuracy
parameters:
  decay:
    values: [0.00001, 0.0001, 0.001]
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}