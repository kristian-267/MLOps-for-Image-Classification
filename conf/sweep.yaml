program: src/models/train_model.py
method: bayes
metric:
  goal: maximize
  name: accuracy
parameters:
  decay:
    min: 0.0001
    max: 0.001
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}